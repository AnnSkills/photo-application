<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <%= link_to 'DeviseDemo', root_path, class: 'navbar-brand' %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to 'Home', root_path, class: 'nav-link active' %>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link active dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Settings
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <% if user_signed_in? %>
              <li><%= link_to 'Sign Out', destroy_user_session_path, class: "dropdown-item", data: { turbo_method: :delete } %></li>
            <% else %>
              <li><%= link_to 'Sign In', new_user_session_path, class: "dropdown-item" %></li>
            <% end %>
          </ul>
        </li>
      </ul>
      <% if user_signed_in? %>
        <li><%= link_to 'Sign Out', destroy_user_session_path, class: "dropdown-item", data: { turbo_method: :delete } %></li>
      <% else %>
        <li><%= link_to 'Sign In', new_user_session_path, class: "dropdown-item" %></li>
      <% end %>
    </div>
  </div>
</nav>
<% if user_signed_in? %>
<%= current_user.email %>
  <p>Mode: <%= current_user.mode %></p>
  <p>Subscription plan: <%= current_user.subscription_status %></p>
  <p>Stripe customer id <%= current_user.stripe_customer_id %></p>
  <a><%= link_to "Pricing", pricing_path  %></a>
  <% else %>
  <%= link_to "Sign Up",  new_user_registration_path, method: :post, remote: true %>
<% end %>

