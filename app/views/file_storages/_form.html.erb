<%= form_with(model: file_storage) do |form| %>
  <% if file_storage.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(file_storage.errors.count, "error") %> prohibited this file_storage from being saved:</h2>

      <ul>
        <% file_storage.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<div>
  <%= form.label :file, style: "display: block" %>
  <%= form.file_field :file %>
</div>
  <div>
    <%= form.submit %>
  </div>
<% end %>
